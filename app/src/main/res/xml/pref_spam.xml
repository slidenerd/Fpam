<PreferenceScreen xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- NOTE: Hide buttons to simplify the UI. Users can touch outside the dialog to
         dismiss it. -->
    <!-- NOTE: ListPreference's summary should be set to its value by the activity code. -->
    <PreferenceCategory android:title="@string/text_links">
        <Preference
            android:key="@string/key_links_whitelist"
            android:summary="@string/pref_summary_links_whitelist"
            android:title="Whitelist" />
        <Preference
            android:key="@string/key_links_blacklist"
            android:summary="@string/pref_summary_links_blacklist"
            android:title="Blacklist" />
    </PreferenceCategory>
    <PreferenceCategory android:title="@string/text_content">
        <Preference
            android:key="@string/key_spam_content"
            android:summary="@string/pref_summary_spam_blacklist"
            android:title="Spam words or phrases" />
    </PreferenceCategory>
    <PreferenceCategory android:title="@string/pref_category_users">
        <Preference
            android:key="@string/key_spammers"
            android:summary="@string/pref_summary_spammers_blacklist"
            android:title="Spammers" />
    </PreferenceCategory>
</PreferenceScreen>
